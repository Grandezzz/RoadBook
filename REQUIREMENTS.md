# 路书需求记录

本文件为后续需求及变更的统一记录入口。

## 历史资料情况

用户于 2026-09-22 明确指出：历史需求文档在 `REQUIREMENTS` 里面，应以该原始资料为准。当前助手在 `D:\AGENT` 下按名称复查，仅能看到上一轮由助手新建的本文件，尚未定位到用户所指的原始文件或目录。不能据此认定历史需求不存在。本文件暂作为本轮记录，定位原始资料后应沿用其结构并合并本轮记录，避免建立重复的需求来源。README 中的“已实现”清单仅作为现状资料，不视为历史需求原文。

## 当前功能概况

来源：2026-09-22 对 README、app.js、index.html、styles.css 的静态检查；尚未进行浏览器交互或 iPhone 真机验收。

- 无依赖的静态网站，以 iPhone Safari 为优先使用场景。
- 多份路书、新建与编辑、起止日期、按天管理交通/游玩/吃饭行程。
- 日期页签、展开日历、当前时间段高亮、极简和复杂查看模式。
- 图片选择、预览、移除及复杂模式多图切换。
- 交通主方案及最多两个备选方案，可在查看时切换。
- 行程状态：未进行、已完成、已取消。
- 浏览器 localStorage 存储及 JSON 导出。

## 需求

### REQ-009 同一账号支持多个设备同时登录

- 提出日期：2026-09-24。
- 来源：用户反馈“现在貌似只支持一个设备登录一个账号，不支持多设备登录一个账号。需要改一下”。
- 用户意图：同一个获批账号可在多个设备保持登录并使用云同步；在一台设备退出时不影响其他设备。
- 验收条件：同一账号能在至少两台设备同时登录并访问同一份云端路书；在任一设备退出后，另一设备仍保持登录且可继续读写；登录状态受 Supabase 会话设置允许多会话的配置控制。
- 状态：进行中（多设备会话修复已部署；用户登录时报“the quota has been exceeded”，正修复浏览器本机存储空间用尽时的登录与云同步回退；Supabase 项目会话设置和跨设备读写仍待实测）。
- 完成内容：应用退出按钮使用仅退出当前设备的 Supabase 本地会话范围，避免一台设备退出时撤销账号其他设备的会话。
- 验证情况：`node --check RoadBook/app.js` 通过。GitHub 提交 `5665f66360119180d59820939efebdad722578c0` 已进入 `main`；Pages 工作流 #21 完成成功；公网页面已加载且显示访问登录门禁。用户表示点击邮箱和密码登录后出现“the quota has been exceeded”；静态检查发现应用将包含 data URL 图片的整份行程保存在 localStorage，而 Supabase Auth 默认也使用 localStorage 保存会话，因此浏览器存储达到上限是目前最可能的原因，尚无客户端错误栈可确认。Supabase Auth 默认允许多个设备会话；若项目启用了“Single session per user”，需在 Supabase Auth 会话设置中关闭该限制。两台设备同时登录及一台退出后另一台仍可读写尚未实测。
- 变更记录 2026-09-24：用户要求发布多设备会话修复；用户在 Chrome 打开 GitHub 后，已提交并部署。
- 变更记录 2026-09-24：用户反馈多设备仍不可用，并报告点击邮箱/密码登录后提示“the quota has been exceeded”。按浏览器存储容量耗尽可能性补充 Supabase Auth 到 sessionStorage 的临时回退，并确保本机缓存写入失败时仍尝试云端保存；发布后需用户实测两设备行为。

### REQ-007 修复 iPhone 15 Pro 页面宽度上的控件重叠

- 提出日期：2026-09-23。
- 来源：用户反馈此前 iPhone 15 适配后宽度仍不正确，部分 UI 重叠。
- 用户意图：手机宽度下页面布局自适应，控件和文字不相互遮挡，保证可读和可操作。
- 验收条件：在 iPhone 15 Pro 逻辑宽度下检查访问页、路书列表、编辑页、极简/复杂查看模式和移动/复制弹窗；页面无横向溢出，按钮、输入框、标题与说明不重叠；长文本换行或在所属区域滚动。
- 状态：进行中。
- 完成内容：表单网格允许窄屏收缩，输入项和弹性布局子项不再以固有最小宽度撑开页面；标题/长内容允许换行；400px 以下减少页面、卡片和切换控件的边距。
- 验证情况：GitHub Pages 工作流 #18 成功，带缓存参数的公网页面已加载。尝试运行 `tests/complex-layout.cjs`，但当前环境缺少 Playwright 模块，移动视口自动回归无法运行；iPhone 真机 Safari 尚未验收。需要用户确认原先重叠的路书页面在手机上是否已改善。

### REQ-008 修复编辑行程页操作按钮无响应

- 提出日期：2026-09-24。
- 来源：用户反馈“更新行程”“返回”和顶部对勾按钮点击无反应。
- 用户意图：编辑行程时上述按钮均能正常操作，并对保存结果给出明确反馈。
- 验收条件：编辑行程后可更新内容；返回按钮可离开编辑页；对勾可保存并返回查看页；无效输入或保存失败时页面保留且显示原因。
- 状态：进行中（按钮绑定与错误反馈修复已部署；需在获批账号的真实编辑页确认操作）。
- 完成内容：返回、对勾保存和更新行程按钮改为在编辑界面生成后立即绑定，避免后续图片/交通方案表单初始化异常导致按钮没有事件；兼容旧的单张图片字段；无效日期和本地存储失败时保留编辑页并显示提示。
- 验证情况：`node --check RoadBook/app.js` 通过；GitHub Pages 工作流 #20 成功；公网页面加载新脚本且无启动错误。当前浏览器没有获批账号会话，无法在受保护的编辑页面真实点击验收；iPhone Safari 真机也待用户确认。

### REQ-006 访问网站前须经所有者批准

- 提出日期：2026-09-23。
- 来源：用户：“那改一下，现在必须我同意才可以访问这个网站”。
- 用户意图：网站访问权限由所有者控制，访客不能自行注册后进入网站；获批账号应便于在手机上登录。
- 实施约定：按仅限受邀/获批账号无密码邮箱链接登录实现；访客可看到登录入口和联系所有者的说明，但在获批账号验证前不能进入路书内容。所有者在 Supabase 中邀请账号并登记获批邮箱；数据库访问策略也校验获批状态，不能只靠隐藏前端页面。
- 验收条件：
  - 未登录或未获批的账号不能查看路书页面或读取云端路书数据。
  - 网站不提供公开自助注册入口。
  - 获批账号通过 Supabase 邀请邮件首次设置密码后，可使用邮箱/密码登录并按 REQ-005 同步其个人路书；未获邀邮箱不得自行注册。
  - README 说明所有者如何邀请/批准访问者。
  - Supabase 配置、SQL 部署及公网行为均须实际完成后再标记已完成。
- 状态：进行中（密码登录和邀请设密入口已部署，并在公网确认显示；实际邀请邮件、密码设置、登录和云同步仍待用户操作验收）。
- 实施结果：应用启动时仅渲染登录/不可用门禁，未获批前不渲染路书页面；不提供应用内自助注册。登录后先检查 `roadbook_access` 获批邮箱，再读取个人路书。SQL 增加仅允许用户查询本人授权记录的策略，并要求路书读写同时具备有效授权。README 补充在 Supabase 邀请账号、增删获批邮箱的所有者操作步骤。
- 变更记录 2026-09-23：用户确认已完成邀请和获批邮箱 SQL；反馈点击邀请链接打开空白页。补充受邀账号设置密码界面，并在配置说明中加入 GitHub Pages 的 Supabase Auth Site URL/Redirect URLs。
- 变更记录 2026-09-23：用户曾提出私人邀请制下希望免密码，已短暂部署一次性登录链接；随后反馈邮件仍发送失败，并明确希望使用初次邀请邮件设置密码、以后使用密码登录。此决定取代一次性登录方式。
- 变更记录 2026-09-23：用户表示“不需要验证”，后续澄清是希望避免登录邮件验证流程，采用受邀账户的密码登录。仍需输入密码证明账户身份；不再考虑向所有访客开放。
- 验证情况：用户确认 Supabase SQL、邀请/获批步骤及 Auth 回跳地址设置成功；GitHub Pages 工作流 #7、#9、#10、#14、#16 成功。公网已确认显示获邀邮箱、密码输入框和登录按钮；真实邀请邮件、密码设置和跨账号隔离尚未实测。

### REQ-001 后续需求同步记录到 Markdown，并写入技能

- 提出日期：2026-09-22。
- 来源：用户本轮对话：“看一下这个网站，之前提了需求在md文件里，之后也要在提需求时记录到md文件里（记入skill）”。
- 用户意图：了解当前网站及现有需求资料；建立持续的需求记录规则，避免后续需求仅留在聊天中。
- 验收条件：
  - 检查当前网站代码及可找到的 Markdown，明确历史资料是否存在。
  - 建立统一需求 Markdown，记录本次要求。
  - 将后续需求记录、需求变更及完成状态维护规则写入项目技能。
  - 项目 AGENTS.md 提供技能与需求文档的读取入口。
- 状态：进行中（记录规则已建立，原始需求文档待定位与核对）。
- 完成内容：新增本文件、`../skills/roadbook-requirements/SKILL.md` 和项目根目录 `../AGENTS.md`；在 README 增加需求记录入口。
- 验证情况：已核对文档路径、技能 frontmatter 和规则内容；本轮仅修改文档，未修改网站业务逻辑，未进行浏览器或真机交互验证。
- 2026-09-22 用户纠正：需求文档在 `REQUIREMENTS` 里面；已更新资料情况，待确认具体路径后读取原文并修正相关入口。

### REQ-002 iPhone 15 Pro 复杂模式一屏适配

- 提出日期：2026-09-22。
- 来源：用户：“做一下iphone 15pro的屏幕适配。现在复杂模式下一个页面内展示不全”。
- 用户意图：复杂模式的单张行程卡片在手机可见区域内完整展示，不被浏览器工具栏或底部安全区域裁切。
- 验收条件：按手机实际可见高度分配页面和图片高度；常规行程的时间、标题、地点、状态和交通备选按钮一屏可见；较长内容可在卡片内部滚动读取；保留图片及行程切换，并避免文字滚动误触切换。
- 状态：已完成（代码及桌面浏览器模拟验证完成，未做 iPhone 真机 Safari 验证）。
- 完成内容：复杂模式改用动态可见视口高度和安全区留白；卡片填满剩余空间，图片区随内容自动伸缩；长文字在卡片内滚动，滚动时不触发行程切换；修正空行程回退极简模式时残留的复杂布局状态。
- 验证结果：Edge 移动视口模拟通过 393×852、393×759、393×659、393×580 下有图/无图共 8 组检查；常规交通行程的状态和三个方案按钮均未裁切，无横向溢出；额外检查模拟安全区、长文字滚动、方案/状态/图片按钮、极简模式及空行程回退，未出现浏览器脚本错误。已查看生成的预览截图。Safari 动态工具栏和真机触摸尚未实测。
- 回归检查：项目根目录 `tests/complex-layout.cjs`（需要 Playwright 和 Edge；可通过 PLAYWRIGHT_MODULE 指定 Playwright 模块路径）。

### REQ-003 单个行程移动/复制到指定日期

- 提出日期：2026-09-22。
- 来源：用户要求在极简和复杂模式的单个行程上各加一个按钮，点击出现移动、复制两个选项，将该行程插入某一天。
- 实现约定：目标为当前路书起止日期内的某一天；保留时间、内容、图片、交通方案和状态；移动删除原日期条目，复制保留原条目并生成独立 ID；插入后按开始时间正序展示，完成后跳到目标日期的行程。
- 验收条件：两种模式均可操作，支持取消；移动至同日不重复插入，复制至同日允许；不覆盖目标日期其他行程；保存后刷新仍保留结果；长内容及手机布局保持可用。
- 状态：已完成。
- 完成内容：两种查看模式的行程卡片新增“移动/复制”按钮；弹窗提供两种操作与目标日期选择，成功后切换到目标日期；复制使用独立数据和新 ID；保存失败时恢复原数据并在弹窗提示。
- 验证结果：`tests/transfer-item.cjs` 在 Edge 移动视口下验证两种模式的移动、复制、取消、插入排序、原内容保留、复制后状态独立、同日复制、空日期插入和刷新保存；单日路书限制、Escape 关闭、存储失败回滚均通过。`tests/complex-layout.cjs` 的四种高度、有图/无图、安全区和长文字回归通过，已查看含新增按钮的截图；未进行 iPhone 真机 Safari 验证。

### REQ-004 免费公网托管

- 提出日期：2026-09-23。
- 来源：用户要求部署到公网、不用本机作为服务器，并寻找免费托管平台。
- 验收条件：使用免费云端静态托管，通过 HTTPS 公网链接在手机打开；电脑关闭后仍可访问，不使用本机服务器或内网穿透。
- 状态：已发布（GitHub Actions 的部署任务成功；浏览器端现场复核因 Chrome 连接中断未完成）。
- 托管方案变更：用户选择 GitHub Pages；本地电脑不作为服务器，也不使用 Cloudflare。
- 公开性提示：用户指定的免费 GitHub Pages 方案要求 public 仓库；提交的网站源代码会公开。
- 数据约束：现有路书仍保存在各浏览器本地，部署不包含云端数据同步。
- 实施结果：已向公开仓库提交 `index.html`、`app.js`、`styles.css` 和 `.github/workflows/pages.yml`；在仓库设置将 Pages 来源切换为 GitHub Actions，并重跑最初因 Pages 尚未启用而失败的任务。重跑结果显示 configure、上传制品和 deploy-pages 步骤全部成功。
- 预期项目站点地址：`https://grandezzz.github.io/RoadBook/`。Chrome 连接中断，本轮未能打开页面确认最终呈现；请用户可直接在手机尝试访问。

### REQ-005 将路书数据保存到云端

- 提出日期：2026-09-23。
- 来源：用户询问是否可以把数据存到数据库或 Git，比较 Supabase 后明确选择：“那就supabase吧，现在做什么”。
- 用户意图：用 Supabase 取代仅限单设备的本地保存方式，使路书可在手机等设备之间同步。
- 实施约定：受邀账号首次通过 Supabase 邀请邮件设置密码，之后以邮箱/密码认证；每个账号仅能访问自己的路书；保留 localStorage 作为本机缓存和迁移来源；不向公开 GitHub 仓库保存用户行程；Supabase URL 和公开 anon/publishable key 仅作为前端配置，数据库 RLS 必须开启。
- 验收条件：
  - 本地数据继续作为缓存/迁移来源；公网应用按照 REQ-006 拒绝未登录或未获批访客。
  - 获批账号通过邮箱/密码登录后，已有本地路书首次写入云端，后续新增或修改保存到账号数据。
  - 同一账号在另一设备登录后可加载云端路书；其他账号无法读取或修改该数据。
  - 页面说明 Supabase 项目配置、SQL 初始化与安全注意事项。
  - 完成 Supabase 项目配置后再发布并做跨设备实测；不得声称尚未实测的同步已验收。
- 安全约束：路书记录可能包含个人行程；不得写入当前公开的 `Grandezzz/RoadBook` 仓库。采用私有数据库时需要身份认证和按用户隔离的访问规则。
- 状态：进行中（Supabase 项目 URL、公开 Publishable key、数据库/RLS 和邮箱获批已配置；邀请设密及邮箱/密码登录已发布；登录及云同步仍待实测）。
- 实施结果：增加 `supabase-config.js`、`supabase-schema.sql` 用户隔离策略、邮箱/密码登录/退出、邀请邮件设密页和强制访问门禁；首次获批账号登录时无云端数据则上传本机路书，有云端数据则加载并保留登录前本机备份。更新 README 配置步骤和安全说明。
- 验证情况：已根据用户提供的项目编号填入对应 API URL，并写入用户提供的 Publishable key；用户确认邀请/授权 SQL 和回跳配置已完成。GitHub Pages 工作流 #16 成功；公网登录页确认显示密码字段和登录按钮。真实邀请邮件、密码设置、邮箱/密码登录及云端同步仍待实测。
- 变更记录 2026-09-23：用户反馈发送密码设置邮件后页面显示失败。原界面隐藏了 Supabase 返回原因；现改为展示其错误详情，并对 HTTP 429/常见限流错误给出等待及额度提示。实际邮件能否送达仍需根据页面新提示判断并由用户验收。
- 验证情况：本地代码静态检查；尚未实际请求 Supabase 邮件接口或真机验收。
- 变更记录 2026-09-23：用户决定受邀者通过初次 Supabase 邀请链接设置密码，后续以邮箱/密码登录；一次性登录链接方式不再作为当前方案。
- 变更记录 2026-09-24：用户反馈点击“邮箱和密码登录”后显示 `the quota has been exceeded`。推测浏览器 Web Storage 已满（路书含图片 data URL 且缓存到 localStorage），加入 Supabase Auth 会话写入失败时回退到 sessionStorage，并允许已登录时本地缓存写入失败仍继续云端读写。若回退到 sessionStorage，关闭标签页后需重新登录；本机缓存空间治理仍需后续处理。
- 变更记录 2026-09-24：按 REQ-010 取消已登录账号继续将图片和整份路书写入本机缓存；图片改存私有 Supabase Storage。旧版本机副本只在与云端原始数据一致且云端保存成功后清理，不同的旧数据仍保留以防丢失。
- 状态：代码修复进行中；登录/云端读写需在用户设备实际验收。

### REQ-010 图片改用云端存储和读取

- 提出日期：2026-09-24。
- 来源：用户：“那不要把图片下载到本地的缓存里，在云上进行读取”。
- 用户意图：避免行程图片占满手机浏览器的本地存储；图片存放在 Supabase 云端，打开行程时读取。
- 验收条件：新图片上传到受账号权限保护的云端对象存储；路书仅保存对象路径，不再将图片 data URL 写入 localStorage 或数据库路书 JSON；已有图片能安全迁移，迁移失败不删除原图；两种设备可查看同一账号的图片；移动/复制行程保留图片。
- 状态：已发布，待获批账号登录及手机跨设备实测。
- 完成内容：新图以文件形式上传私有 Supabase Storage，路书仅存对象路径；查看和编辑预览按需获取短期有效的私有链接；旧 data URL 在登录时迁移，上传或云端保存失败时保留原数据；与云端原始数据一致的本机旧副本在迁移后清除。已在项目中创建私有 `roadbook-images` 图片桶和两条按获批账号及用户路径限制的策略。
- 验证情况：`node --check RoadBook/app.js` 通过；`tests/cloud-images.cjs` 覆盖上传路径、迁移成功、迁移失败回滚和图片链接缓存；Supabase 控制台显示图片桶、2 条策略和 10 MB 限制；GitHub Pages 公网脚本已包含云存储代码。没有获批账号的登录凭证，本轮无法实测真实图片上传与手机跨设备显示。浏览器正常的临时网络缓存不在应用控制范围内。
- 反馈记录 2026-09-24：用户先反馈“所有图片都不显示了”，随后纠正为“图片现在显示的非常慢”。应排查私有图片链接获取与图片文件加载耗时，优化手机查看和多图切换的等待时间；现有图片数据须保持安全。
- 处理记录 2026-09-24：手机首次打开和每次切图均慢。优化方向为批量准备相册图片链接、预读相邻图片；新 JPEG/PNG/WebP 上传前在内存中缩至最长边 1600 像素，已有云端大图在首次查看后生成最长边 1100 像素的私有预览图。原图保留于云端，预览生成失败时继续显示原图。首次处理旧图仍需读取一次原图，不保证首次立即变快。
- 状态：优化代码已完成，待发布并用获批账号在手机验证实际加载速度。

## 代码检查发现（非用户已确认需求）

以下为助手静态检查所得，尚未实施修改，也不代表原始需求遗漏。

- 日期校验：保存按钮未处理 `syncBook()` 返回 false 的情况，日期顺序校验失败后仍会保存并退出；空日期也未被拦截。后续修复应验证无效日期下留在编辑页并保留输入。
- 备份恢复：当前提供 JSON 导出，未发现导入入口；是否需要恢复功能待用户提出或确认。
